import styled from "@emotion/styled";
import { size, color, layout, flex, space } from "styled-system";
import { Box } from "rebass";

const Main = () => {

  return (
    <Box
      as='main'
      bg='white'
      flex='2'
    >Main</Box>
  )
}

export default Main